<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sslab.samplegroupapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ThreadMessageQueueSample" />
        <activity android:name=".Sample02Activity" />
        <activity android:name=".CustomTextViewActivity" />
        <activity android:name=".GridViewSample" />
        <activity android:name=".MultiLineTextListViewActivity" />
        <activity android:name=".ProgramaticallySettingLayoutHeightActivity"></activity>
    </application>

</manifest>